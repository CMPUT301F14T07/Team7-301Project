Class Names

SCREENS
-------

HomeActivity

AskActivity

BrowseActivity

QuestionActivity


DataManger
    Methods:
        load()
        save(PostList postList)

ForumEntryList:
    Methods:
        add(Question q)
        sortByTime()
        sortByRating()
        sortByHasPicture()
        sortBySearchTerm()
        getlist()
        changeRating(int index,int new_rating)
        addPicture(int index)
        
        
Reply
    Methods:
        setReply(String s)
        getReply()

Post
    Methods:
        getReplySpecific(int id)
        addReply(Reply reply)
        getReplies()
        getUpVote()
        setUpVote(int upVote)
        getDownVote()
        setDownVote(int downVote)
        getPost()
        setPost(String post)
        getPostersName()
        setPostersName(String postersName)
        

ForumEntry
    Methods:
        getQuestion()
        addAnswer(Post answer)
        getAnswer(int postPosition)
        upVoteQuestion(int deltaVote)
        downVoteQuestion(int deltaVote)
        getUpVoteOfQuestion()
        getDownVoteOfQuestion()
        upVoteAnswer(int postPosition, int deltaVote)
        getUpVoteOfAnswer(int postPosition)
        downVoteAnswer(int postPosition, int deltaVote)
        getDownVoteOfAnswer(int postPosition)
        addReplyToQuestion(Reply reply)
        getRepliesToQuestion()
        getRepliesToAnswer(int postPosition)
        addReplyToAnswer(Reply reply, int postPosition)

Controller

Search
    Methods:
        getSearchTerm()
        sortBySearchTerm()
        
